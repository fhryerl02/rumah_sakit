<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* Reset default margin and padding */
body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    background-color: #f5f5f5; /* Light gray background for the page */
    color: #333; /* Dark text color for readability */
}

h2 {
    text-align: center;
    color: #0074D9; /* Blue header color */
    margin-top: 20px;
}

form {
    max-width: 500px;
    margin: 20px auto;
    padding: 20px;
    background-color: #ffffff; /* White background for the form */
    border-radius: 8px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Subtle shadow for depth */
}

label {
    display: block;
    margin-bottom: 8px;
    font-weight: bold;
    color: #0074D9; /* Blue label color */
}

input[type="text"],
select {
    width: 100%;
    padding: 10px;
    margin-bottom: 15px;
    border: 1px solid #ccc; /* Light gray border */
    border-radius: 4px;
    font-size: 14px;
    box-sizing: border-box; /* Ensure padding doesn't affect width */
}

input[type="text"]:focus,
select:focus {
    border-color: #0074D9; /* Highlight border on focus */
    outline: none;
    box-shadow: 0 0 5px rgba(0, 116, 217, 0.5); /* Soft blue glow on focus */
}

button[type="submit"] {
    display: inline-block;
    width: 100%;
    padding: 10px;
    background-color: #0074D9; /* Blue button background */
    color: #fff; /* White text */
    font-size: 16px;
    font-weight: bold;
    border: none;
    border-radius: 4px;
    cursor: pointer;
    transition: background-color 0.3s ease; /* Smooth hover effect */
}

button[type="submit"]:hover {
    background-color: #0056b3; /* Darker blue on hover */
}

option {
    background-color: #ffffff; /* White background for dropdown options */
    color: #333; /* Dark text for dropdown options */
}
    </style>
</head>
<body>
    <h2>Tambah Dokter</h2>
<form action="/dokter/store" method="POST">
    <label for="id_pegawai">Pegawai:</label>
    <select name="id_pegawai" id="id_pegawai" required>
        <% if (pegawai && pegawai.length > 0) { %>
            <% pegawai.forEach(pg => { %>
                <option value="<%= pg.id_pegawai %>"><%= pg.nama_pegawai %></option>
            <% }) %>
        <% } else { %>
            <option value="">Tidak ada pegawai tersedia</option>
        <% } %>
    </select>

    <label for="nama_dokter">Nama Dokter:</label>
    <input type="text" name="nama_dokter" id="nama_dokter" required>

    <label for="status_dokter">Status Dokter:</label>
    <input type="text" name="status_dokter" id="status_dokter" required>

    <button type="submit">Simpan</button>
</form>
</body>
</html>
